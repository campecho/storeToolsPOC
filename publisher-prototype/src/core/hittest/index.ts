/**
 * Hit-testing barrel (PLAN.md §6.1 `core/hittest/`) — pure functions per the
 * ToolContract HitTestSpec, plus the AABB math the overlay and gesture
 * contexts consume.
 */
export * from "./geometry";
export * from "./aabb";
export * from "./hitTest";
